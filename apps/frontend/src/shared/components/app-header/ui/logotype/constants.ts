import { ORGANIZATION_NAME as APP_ORGANIZATION_NAME } from '@/shared/lib/constants';

export const ORGANIZATION_NAME = APP_ORGANIZATION_NAME;

export const DEFAULT_IMAGE_SOURCE = '/images/logo.png';
export const DEFAULT_IMAGE_ALT_TEXT = `Logotype of ${ORGANIZATION_NAME}`;
